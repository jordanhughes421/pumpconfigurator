{
  "name": "magnum-opus",
  "version": "0.1.0",
  "private": true,
  "description": "Pump selection, configuration, and performance modeling application",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
